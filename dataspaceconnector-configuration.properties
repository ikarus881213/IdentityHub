# IdentityHub Configuration
web.http.identity.port=8182
web.http.identity.path=/api/identity
web.http.credentials.port=10001
web.http.credentials.path=/api/credentials

# Issuer Service Configuration (Integrated)
web.http.issuance.port=8283
web.http.issuance.path=/api/issuance
web.http.issueradmin.port=8383
web.http.issueradmin.path=/api/issuer
web.http.statuslist.port=9999
web.http.statuslist.path=/statuslist

# Common Configuration
edc.jsonld.vocab.disable=true
edc.hostname=localhost

# Database Configuration (PostgreSQL)
edc.datasource.default.url=jdbc:postgresql://localhost:5432/identityhub
edc.datasource.default.user=postgres
edc.datasource.default.password=password
edc.sql.schema.autocreate=true

# Security & Vault Configuration
edc.vault.hashicorp.url=http://localhost:8200
edc.vault.hashicorp.token=hvs.hp37V9GLqBD1tIPdoxYSXzBZ
edc.vault.hashicorp.timeout.seconds=30

# Super User Configuration
edc.ih.api.superuser.key=c3VwZXItdXNlcg==.password1234
edc.ih.api.superuser.id=super-user

# Signing Keys
edc.issuer.statuslist.signing.key.alias=statuslist-key

# Disable strict encryption mode for development
edc.encryption.strict=false

# Allow HTTP for did:web resolution (Development only)
edc.iam.did.web.use.https=false